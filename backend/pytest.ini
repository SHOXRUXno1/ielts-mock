[pytest]
# Without this, pytest also scans app/, where model modules such as
# app/models/test.py and app/models/test_section_settings.py match the
# default test_*.py discovery pattern.
testpaths = tests
